#include "main.h"


/**
* _strlen - Returns the length of a specified string.
* @str: Specified string to ge the length value
*/

size_t _strlen(const char *s)

{
	size_t length = 0;

	while (*str++)
	length++;
	return (length);
}
